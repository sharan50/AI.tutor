# AI.tutor economic instrument

Not a forecast. An instrument for ranking levers under stated ignorance.

Seed 20260916, run date 2026-09-16, 20000 paths, 60 monthly steps, USD.

    python3 model.py      writes out/por_monthly.csv and out/por_paths.csv
    python3 harness.py    refuses unless it rebuilds both, character for character

Everything downstream runs through `harness.load()`, so it runs the published
code rather than a restatement of it. `out/por_paths.csv` is regenerated by
`model.py` and is not committed; every other output is.
